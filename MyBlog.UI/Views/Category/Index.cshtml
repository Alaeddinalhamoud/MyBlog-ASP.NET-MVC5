@model IEnumerable<MyBlog.Data.Category>
@using GridMvc.Html

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Dashbord.cshtml";
}


<link href="~/Content/reveal.css" rel="stylesheet" />

<div class="panel panel-default">
    <div class="panel-heading">

        <div><span class="fa fa-list fa-2x" aria-hidden="true"></span> All Categories</div>
    </div>
    <div class="panel-body">

        <div class="code-cut table table-responsive">
            @Html.Grid(Model).Columns(columns =>
       {
           columns.Add(c => c.CategoryName).Titled("Category Name").Filterable(true);
           columns.Add(c => c.Frequence).Titled("Freq.").Filterable(true);
           columns.Add(c => c.Create_time).Titled("Creation Time").Filterable(true);
          
           columns.Add().Encoded(false).Sanitized(false).SetWidth(30).RenderValueAs(o => Html.ActionLink("Edit", "Edit", new { id = o.CategoryId }));
          
           columns.Add().Encoded(false).Sanitized(false).SetWidth(30).RenderValueAs(o => Html.ActionLink("Delete", "Delete", new { id = o.CategoryId }));

       }).WithPaging(10).Sortable(true)
        </div>


    </div>
    <div class="panel-footer">
        @Html.ActionLink("New Category", "NewCategory", null, new { @class = "btn btn-primary fa fa-plus"  })
     
    </div>
</div>






